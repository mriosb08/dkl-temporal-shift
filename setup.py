#!/usr/bin/env python
from setuptools import setup, find_packages

setup(name='mimic3benchmark',
      version='0.0',
      description='mimic3benchmarks',
      packages=find_packages(),
      include_package_data=True
)
